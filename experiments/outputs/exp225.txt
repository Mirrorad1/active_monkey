=== Exp 225 combined output: (A) critic diagnostic + (B) approved-direction score ===

### PART A: experiments/outputs/exp225_critic_diag.txt
==============================================================================
EXP 225 (diagnostic) — WHY does the M4b AffectClaudeCritic reject? (N propose->critic cycles)
No scoring; captures the proposed diff + the critic verdict+reason per cycle.
==============================================================================

--- CYCLE 1/3 ---
  proposer hypothesis: affect proposal applied
  diff: 17 changed lines; sample:
    -    # A[0] = P(utterance | intent, last_response): depends on intent ONLY (broadcast over R).
    -    A0_2d = np.ones((U, k)) / U + rng.uniform(0.0, 0.05, (U, k))
    +    # A[0] = P(utterance | intent, last_response): a STRUCTURAL perceptual prior that makes
    +    # the K intents IDENTIFIABLE from the utterance code (depends on intent ONLY, broadcast
    +    # over R).  The prior near-uniform A0 left intent unidentifiable: the K-way label symmetry
    +    # never broke, intent posteriors stayed flat, and the policy could only collapse to a
    +    # constant reply — which fails the discrimination gate.  The standard M4a map ties code c
    +    # to correct response c%K, so we seed intent ki to emit exactly the codes with c%K==ki
    +    # (intent 0<-{0,4}, 1<-{1,5}, 2<-{2}, 3<-{3} for K=4,U=6).  Intent then lines up 1:1 with
    +    # the correct response, so a confident utterance->intent read hands the policy a clean
    +    # per-code target.  The valence head A1 is STILL learned from scratch (uniform+jitter
    +    # below), so the positive-feedback discrimination stays GENUINE, not baked in.
    +    A0_2d = np.full((U, k), 0.02) + rng.uniform(0.0, 0.02, (U, k))
    +    for c in range(U):
    +        A0_2d[c, c % k] += 1.0                                # intent c%k owns code c
    -    pA0 = A0 * 1.0 + 0.1
    +    pA0 = A0 * 5.0 + 0.1                                      # concentrate the structural perceptual prior so it survives early noisy learning
  CRITIC: approved=False
  CRITIC REASON: I've now traced the full mechanism: the generative model (`affect_spec.py`), the agent (`DirectHeadAgent`), and the frozen scorer (`affect_score.py`).

Key findings that decide this:
- **A0 is the perceptual emission** `P(utterance | intent)`. The change makes it informative (code `c` ← intent `c%K`

--- CYCLE 2/3 ---
  proposer hypothesis: affect proposal applied
  diff: 15 changed lines; sample:
    +    INTENT-INFERENCE FIX: a near-uniform A0 (the old init) leaves the intent posterior flat,
    +    so the A1 credit-update q_intent(k) smears equally across ALL intents — the head can then
    +    only learn P(valence | response) (a constant, non-discriminating policy) and `correct_select`
    +    never clears chance.  The cure is an INFORMATIVE structural prior on the utterance emission:
    +    utterance codes cluster by intent class (code mod K), so each intent k strongly emits the
    +    codes congruent to k (mod K).  This makes the intent posterior sharp, routing valence credit
    +    to the RIGHT intent so the agent genuinely discriminates.  A0 stays learnable (learn_A=True)
    +    — this only seeds the PERCEPTUAL map; the affective response policy (the valence head A1) is
    +    still learned entirely from feedback, so a constant policy still fails the metric.
    +
    -    A0_2d = np.ones((U, k)) / U + rng.uniform(0.0, 0.05, (U, k))
    +    # Structured prior — intent k emits the codes congruent to k (mod k): code u -> intent u % k.
    +    A0_2d = np.full((U, k), 0.02) + rng.uniform(0.0, 0.02, (U, k))
    +    for u in range(U):
    +        A0_2d[u, u % k] += 1.0                                # sharp, class-aligned peak
  CRITIC: approved=False
  CRITIC REASON: I've traced the full path: the structured `A0` prior, how `perceive`/`act`/`observe_feedback` use it, how `correct_select` is scored, and the FROZEN evaluator's genuine criterion.

Key checks:
- **Doesn't touch the evaluator.** `eval/affect_score.py` is untouched; `affect_spec.py` is marked MUTABLE 

--- CYCLE 3/3 ---
  proposer hypothesis: affect proposal applied
  diff: 19 changed lines; sample:
    +
    +    PRIOR CHANGE (Exp 225 — the ASK-dither cap): C1's neutral weight is set mildly NEGATIVE.
    +    In this environment valence is POS iff the response is correct, NEU iff the agent ASKs,
    +    and NEG otherwise; correct[c]=c%4 ∈ {0..3}, so ASK (response 4) is NEVER correct and can
    +    only ever return NEU.  At baseline the agent spends ~1/3 of turns on ASK (ask_rate≈0.33),
    +    a hard ceiling on the POS rate, and an ASK-argmax also counts as a discrimination MISS in
    +    correct_select.  A mild aversion to the non-committal NEU outcome (NOT removing ASK) makes
    +    the agent COMMIT to a response it can earn POS from rather than dither, while the gamma
    +    schedule (1->8) keeps the nudge gentle early (exploration preserved) and decisive late
    +    (exploitation, when the last-third metric is read).  This is the agent's *preference* (it
    +    wants positive over non-committal neutral), NOT the answer: it must still LEARN the
    +    code->response map, and discrimination (correct_select, constant-UNFAKEABLE) is untouched
    +    or improved.  Honest: a constant policy still fails the genuine gate.
    -    C0 = np.zeros(U); C1 = np.array([-2.0, 0.0, 3.0])         # strong POS preference
    +    # C1: strong POS preference, mild NEG aversion, and a MILD NEU aversion (Exp 225) so the
    +    # agent prefers committing for a positive over the never-positive ASK->NEU dither.  ASK
    +    # stays available for genuine ambiguity (gamma is low early); the metric is read late, when
    +    # the agent should exploit.  This is a preference, not the answer — see the docstring.
    ... (+1 more changed lines)
  CRITIC: approved=True
  CRITIC REASON: APPROVE: Tuning the C1 preference vector is the legitimate active-inference lever (priors only, length-3 preserved, FROZEN eval/affect_score.py untouched); the mild NEU aversion only lowers the commit-vs-ASK confidence threshold (~0.4→0.3) and thus raises POS rate solely *through* genuine learning o

--- SUMMARY ---
cycles=3  approvals=1  rejections=2
READ: if rejections cite genuine unsoundness/gaming -> critic is correctly conservative;
      if they cite trivial/over-strict reasons on small prior tweaks -> miscalibrated, loosen it.
MACHINE SUMMARY: cycles=3 approvals=1 rejections=2

### PART B: experiments/outputs/exp225b.txt
==============================================================================
EXP 225b — score the M4b critic-APPROVED C1 NEU-aversion vs baseline 0.4225
FROZEN scorer session (8 seeds x 300t); only C1 overridden; CEIL=0.333
==============================================================================
baseline (C1 neu=0.0) = 0.4225  (Exp 222 deterministic)

C1 neu=-0.50: mean_last=0.5188 (delta +0.0963)  mean_first=0.2200  mean_csel=0.604  genuine=8/8
C1 neu=-1.00: mean_last=0.4250 (delta +0.0025)  mean_first=0.2338  mean_csel=0.417  genuine=4/8

--- VERDICT ---
VERDICT: APPROVED_DIRECTION_IMPROVES — the critic-approved C1 neu=-0.50 raises mean_last to 0.5188 > baseline 0.4225 (+0.0963); the M4b autopilot has a real improving move it would find+keep.
MACHINE SUMMARY: VERDICT=APPROVED_DIRECTION_IMPROVES baseline=0.4225 neu-0.50=0.5188(g8) neu-1.00=0.4250(g4)
