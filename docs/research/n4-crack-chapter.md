# The N4 Crack Chapter — the fixed-H squeeze, its closure, and the ambiguity bound

**Status:** CHAPTER SYNTHESIS, 2026-06-12. This chapter ran on the human's explicit
words (the reopening directive of 2026-06-12, the "a" confirmation word, and the
single-regulated-controller directive), executing the program supplied at
`docs/research/n4-fixed-h-squeeze-plan.md`. It COMPLEMENTS the closed N4 chapter
(`docs/research/n4-identity-commitment-chapter.md`) — that chapter's verdict (rung 3
NEGATIVE-config) stands untouched; this chapter resolves its §6 deferral (the
seed-229 crack) by measurement. Every number below is from the committed scripts,
outputs, and rows JSON of the Exp 183 addendum and Exp 184–188; every gated verdict
was blind-verified (PROTOCOL 4.5) and every run was licensed by an equivalence gate
that bit-matches the committed Exp 183 rows (the L15 evidence-table discipline).

**Honesty header.** "Identity" means the creature's value vector and its ordering —
functional policy-continuity, no selfhood claims. Every instrument and controller
FORM is PROVIDED; the creature self-forms only its values, drift history, and
mismatches. All experiments ran on forks; the spines (mirro/vela/nira) were never
touched. "At this body" qualifies every law: the constants are body-specific even
where the functional forms are general.

---

## 1. Executive verdict

The deferred seed-229 crack was **real, narrow, and is now closed constructively at
normal tolerance — with an exact information-theoretic boundary left standing.**

1. **The crack is real (Exp 186, BREAKTHROUGH).** Nine attack schedules confirmed at
   fresh-seed confirmation grade where NO constant in the frozen-time family (nor
   its simple-config extensions) passes both defense and revision, while the oracle
   defends everywhere — defense and revision provably separate.
2. **The crack closes on a different clock (Exp 187).** A stopwatch on the
   CONTINUOUS-pressure clock (INT-C2900: gaps reset it; changed worlds never pause)
   passes both bars in 9/9 cells at normal tolerance.
3. **Regulation ties that clock exactly (Exp 188).** One online regulated controller
   (REG-TB — no oracle, no cell-specific tuning) also passes 9/9, at identical
   per-cell defense, and its online tempo learning never bound a single decision.
   The single-stretch ambiguity bound stands: at fixed-burst-length geometry,
   judgment cannot beat the right clock.
4. **The refrain of the parent chapter completes at a third level:** write-gating
   failed (181), freezing sufficed (182), continuity-timing concedes (187/188). At
   every level the missing piece was the measured quantity — never judgment. The
   universal-constant law (Exp 173) is REFINED, not broken: constants cover
   everything tried, on the right surface.

## 2. The question and the mathematical target

From the supplied plan: does the seed-229 repeated-burst-color failure point to a
regime where no single fixed horizon H can defend transient attacks while still
revising under sustained evidence? A fixed H works iff

    H >= max_b ( L_attack_b - d_trigger_b )      (defense)
    H <= min_r ( T_revision_tolerance_r )        (revision)

The crack exists only if that interval is empty, or if repeated-color cumulation
defeats all fixed-H arms across the declared bars.

## 3. The experiment arc

- **The seed-229 autopsy (Exp 183 addendum; diagnostic, 17/17 bit-match licensed,
  blind-verified).** Mechanism PINNED: every failing burst pays its erosion entirely
  in the ~75-step undetectable head (head_frac = 1.000, with the frozen-plateau
  constancy asserted — v is bit-constant across all in-freeze snapshots; seed 229's
  doses 72.6/72.4/71.5 units at ~0.95–0.97 units/step; cross-seed head erosion
  ~49–100 — CORRECTION from this synthesis's fact-check: the Exp 183 addendum
  entry quotes "~50–97", but the committed autopsy rows run 48.9 to 100.02
  (seed 233 b2); the per-step rate is artifact-verified for seed 229 only).
  Repetition is NOT the discriminant (refuter d:
  all 8 H1200 seeds have repeats; seeds 227/228/233 share 229's exact [1,1,0]
  schedule and pass). Recovery DECOUPLES from burst-end gap in both directions
  (fails at +5.2 and +45.0; passes at +1.0 and −33.5; every observed gap_end ≥ ~80
  recovered) — post-release dynamics decide sub-~80 margins. Found object: seed
  227's burst 0 was frozen-at-onset by a stray pre-burst alarm — zero dose paid.
- **The squeeze map (Exp 184, EXPLORATORY, blind-verified PARTIAL with both flagged
  gaps closed).** 80 schedule cells (L × K × G) × 11 arms × 3 seeds = 2,640
  sessions. Commitment-as-config does NOT extend across schedule space: 36/80
  (normal tolerance), 57/80 (tight), 32/80 (loose) candidate cells with the oracle
  3/3 everywhere. Two laws: the Phase-R latency law (latency = H + baseline,
  EXACTLY — the revision tolerance IS the H-ceiling) and the FREEZE-SPANNING law
  (P1's informative falsification, 8 cells: at short gaps a long H sleeps through
  the gaps and pays ONE detection dose for several bursts — H controls the dose
  COUNT).
- **Classification + dissolution (Exp 185, blind-verified CONFIRMED — all 27
  dissolution claims audited).** Most of the map dissolves: CALM2600 (the
  freeze-spanning law as a config) rescued 24 cell×mode pairs; H9000 rescued ZERO
  (the latency law makes route-1 emptiness arithmetic); 22/36 normal-mode
  candidates were single-seed noise. The surviving hard core: 6 cells (normal),
  35 (tight), 3 (loose).
- **The confirmation (Exp 186, BREAKTHROUGH, blind-verified CONFIRMED).** 6/6 core
  cells confirm on fresh seeds (8/cell; bars: defense ≥ 6/8, revision ≥ 6/8,
  oracle ≥ 7/8, deficit ≥ 7/8) and the variance sample resolves both ways
  honestly: the (1200,1,·) anomaly was noise (all covered — the autopsy's
  election-variance reading vindicated), and three triaged-marginal cells are
  cracks too. THE CONFIRMED SET IS NINE CELLS. Sharpest datum: THETA3-H3000
  defends 7/8 in (2400,4,600) — the family's best showing anywhere — and STILL
  fails revision.
- **Constants on the continuity surface (Exp 187, MIXED, blind-verified
  CONFIRMED).** INT-C2900 closes 9/9 at normal tolerance; the C-sweep traces the
  interval law with zero exceptions (C1500 4/9; C2000 fails exactly the three
  L=2400 cells; C2900 9/9): a working C must exceed the longest unbroken pressure
  stretch a train can produce (~L + the 200-step pressure window) while staying
  within the revision tolerance. Both mechanism predictions for the
  evidence-integral form failed honestly (F1, F3) — including a named
  instrument-fidelity gap (the accumulator counted quiet evidence; the
  pre-registered pressure-gated form went untested there).
- **The regulated controller (Exp 188, POSITIVE-SINGLE, blind-verified; all four
  verifier findings adopted).** REG-TB (concede when the current continuous-ACTIVE
  stretch exceeds max(1.5 × longest completed stretch observed, T₀ = 2800); global
  constants; quiet contributes nothing — 0 leaks audited in 392 sessions) passes
  9/9 — at EXACT defense parity with INT-C2900 (identical 6/8 per cell; the same
  two margin-class seeds fail every arm). Its tempo learning never bound a
  decision. The properly-gated E-form was retired in the same run: 0/9, mid-burst
  surrender in every L ≥ 800 cell (at ~372 active steps — not the predicted ~628;
  the accumulation-rate accounting is an open question).

## 4. Laws and durable findings (this chapter's additions)

- **Dose-ledger law.** Defense failures under fixed-H trace to the irreducible
  detection-head dose (~75 steps; ~0.94–1.0 units/step) cumulating against the
  favorite's margin; repetition matters only as one way margins get pre-eroded.
  Proven by the frozen-plateau constancy read (the L13 pattern).
- **Election decoupling.** Recovery is decided by post-release expression dynamics,
  not the burst-end gap (dissociations both directions; the ~80-unit slack
  regularity, 24 bursts × 8 seeds, one arm family — observed regularity, not a
  tested law).
- **Freeze-spanning / dose-count law.** In trains, H controls how many head doses
  are paid (via the release rule), interacting with G.
- **The unified coverage law.** Spanning arms defend iff H ≥ K·L + (K−1)·G − 75
  (validated at 84.2% fit, predeclared bar 80%).
- **The revision-latency law.** Phase-R latency = H + baseline, exactly — so the
  tolerance IS the H-ceiling, and route-1 interval emptiness is arithmetic.
- **The continuity-surface interval law.** Working C ∈ (max continuous stretch
  ≈ L + window, tolerance] — traced with zero exceptions across the C-sweep.
- **The surrender-schedule law, gated form.** Any finite evidence integral
  reachable within one burst surrenders mid-burst even with gap-resets (E*=600
  falls at ~372 active steps).
- **The single-stretch ambiguity bound.** An online controller with no
  attack-length priors sees identical evidence prefixes for a long attack and a
  changed world; it must hold ambiguous first stretches up to its revision budget,
  so at fixed-L geometry it can match but never beat the tolerance-bounded
  constant. (An information argument; Exp 188 instantiates it as exact parity.)
- **The sufficient-surface law, completed.** Across write-gate → freeze →
  concession, capability arrived each time by measuring the right quantity, never
  by adding judgment.

## 5. Honest limitations

- All bars, tolerances, THETA, calm lengths, κ, T₀, E* are PROVIDED; "no
  cell-specific tuning" means global constants, not no constants.
- "At this body / richness / trigger family" qualifies everything; the two
  inequalities' constants are body-specific though their forms are general.
- The confirmed crack set's boundary outside the 14 retested cells remains
  exploratory-grade (3 seeds); the V class (54 cell×mode pairs) is unresolved by
  construction.
- Two of the six core cells dissolve under loose tolerance; the crack is
  tolerance-relative there.
- Spanning arms are still FROZEN at the final scoring window (calm 2600 > the
  2,500-step post-train tail): the revision bar rules out rigidity, but
  post-release retention is untested at these session lengths.
- The uniform 6/8 defense ceilings in Exp 188 trace to two seeds failing in every
  arm (margin/election class); two Phase-R passes were vacuous (the monitor never
  fired — the regime color matched the standing favorite).
- The E-form accumulation rate (372 vs ~628 active steps to 600 units) is an open
  accounting question, flagged in the Exp 188 entry; it does not affect any
  pass/fail conjunct (verifier-recomputed).
- Exp 187's INT-E600 negative binds its implemented (quiet-leaking) semantics; the
  properly-gated form was tested and retired only in Exp 188.

## 6. Open edges (each needs its own explicit word)

1. **Variable-L attack geometry** — the one regime where regulation could still
   earn its keep: attack lengths varying beyond any single budget force a choice no
   constant can make, and REG-TB's tempo learning would finally have to bind (the
   Exp 173 multi-tempo move at the identity level).
2. **The tight-tolerance core** — where tolerance < a single burst's stretch, NO
   surface tried covers; an exactly-characterized impossibility candidate.
3. **Post-release retention** — score defense after release (longer tails), testing
   whether spanning defenses retain identity once unfrozen.

## 7. Reproduce

Scripts: `experiments/exp183_seed229_autopsy.py`,
`experiments/exp184_n4_fixed_h_squeeze_map.py` (+ `exp184_gate_audit.txt`),
`experiments/exp185_n4_crack_classification.py`,
`experiments/exp186_n4_core_confirmation.py`,
`experiments/exp187_n4_intermittency_controller.py`,
`experiments/exp188_n4_regulated_controller.py`. Raw outputs and rows under
`experiments/outputs/` (exp183_seed229_autopsy, exp184–exp188). Pre-registrations:
commits 8eb3d7a (card + plan), c220d2c (184), 8cc5cdb (185), e3e282e (186),
e1bda4e (187), 3ecfc77 (188) — each precedes its data. Every gated verdict
blind-verified; every run equivalence-gated against the committed Exp 183 rows.
